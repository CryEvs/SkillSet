{
  "name": "@skillset/imessage",
  "version": "2026.4.20",
  "private": true,
  "description": "SkillSet iMessage channel plugin",
  "type": "module",
  "devDependencies": {
    "@skillset/plugin-sdk": "workspace:*"
  },
  "skillset": {
    "extensions": [
      "./index.ts"
    ],
    "setupEntry": "./setup-entry.ts",
    "channel": {
      "id": "imessage",
      "label": "iMessage",
      "selectionLabel": "iMessage (imsg)",
      "detailLabel": "iMessage",
      "docsPath": "/channels/imessage",
      "docsLabel": "imessage",
      "blurb": "this is still a work in progress.",
      "aliases": [
        "imsg"
      ],
      "systemImage": "message.fill",
      "cliAddOptions": [
        {
          "flags": "--db-path <path>",
          "description": "iMessage database path"
        },
        {
          "flags": "--service <service>",
          "description": "iMessage service (imessage|sms|auto)"
        },
        {
          "flags": "--region <region>",
          "description": "iMessage region (for SMS)"
        }
      ]
    }
  }
}
