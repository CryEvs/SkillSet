apiVersion: v1
kind: Service
metadata:
  name: skillset
  labels:
    app: skillset
spec:
  type: ClusterIP
  selector:
    app: skillset
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
