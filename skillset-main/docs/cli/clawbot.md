---
summary: "CLI reference for `skillset clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `skillset clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `skillset clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `skillset clawbot qr` (same behavior as [`skillset qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `skillset clawbot qr` -> `skillset qr`
