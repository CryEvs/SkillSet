# SkillSet Frontend

Standalone frontend for SkillSet that uses the existing backend runtime.

## Run

```bash
cd skillset-frontend
npm install
npm run dev
```

## Notes

- This frontend is isolated from the existing dashboard code.
- Backend remains unchanged.
- Configure WebSocket URL and token in the top form.
